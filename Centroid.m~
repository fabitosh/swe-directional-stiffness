function [pos] = Centroid(maskedVol)
m1mask = maskedVol == -1;
maskedVol(m1mask) = NaN;
pos = nanmean(maskedVol');

m1counter = length(find(maskedVol(~nanmask) == -1));
sumvol = sum(maskedVol(~nanmask)) - m1counter;
disp(sumvol)
end

